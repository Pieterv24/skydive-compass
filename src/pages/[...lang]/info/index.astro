---
import { useTranslatedPath, buildStaticPaths } from '../../../i18n/utils';

export async function getStaticPaths() {
  return buildStaticPaths();
}

const translatePath = useTranslatedPath(Astro.url);

return Astro.redirect(translatePath('/info/manufacturers'), 301);
---
